server:
  port: 8080
spring:
  application:
    name: bmi-service
  ai:
    openai:
      api-key: ${GROQ_API_KEY}
      base-url: "https://api.groq.com/openai"
      chat:
        options:
          model: "llama3-70b-8192"